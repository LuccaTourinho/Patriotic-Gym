import * as React from 'react';

interface IBrandsProps {
}

const Brands: React.FunctionComponent<IBrandsProps> = (props) => {
  return (
    <section className='h-[25vh] ' id='contact'>
      Brands
    </section>
  );
};

export default Brands;
