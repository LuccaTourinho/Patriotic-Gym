import * as React from 'react';

interface IClassesProps {
}

const Classes: React.FunctionComponent<IClassesProps> = (props) => {
  return (
    <section className='h-screen' id='classes'>
        Classes
    </section>
  );
};

export default Classes;
